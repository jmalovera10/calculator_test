{
  "name": "calculator-test-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Cross-service end-to-end tests for the calculator app (Playwright)",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:docker": "docker compose --profile e2e up --build --abort-on-container-exit --exit-code-from e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "wait-on": "^8.0.1"
  }
}
